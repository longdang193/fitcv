## Summary
- centralize langgraph runtime routing defaults under control-plane contract
- keep env vars as override-only channel with explicit precedence/fail-fast behavior
- align docs, plan, and context-pack evidence for lane closeout
- stabilize legacy config-path tests used in bounded verification

## Verification
- python -m pytest -q tests/test_fitcv_cp/test_control_plane_config.py
- python -m pytest -q tests/test_config.py -k "control_plane or routing or legacy or config or path"
- python scripts/hooks/run_validator.py --fast
- py scripts/validate_checkpoint_packs.py
- py scripts/validate_template_required_sections.py

## Notes
- strict planning lifecycle warnings are pre-existing repo-wide manual-linkage warnings outside this lane scope
