stage_id: sample_stage
name: Sample Stage
status: active
purpose: Run the sample stage.
primary_features:
  - zeta-feature
  - alpha-feature
supporting_features:
  - support-z
  - support-a
inputs:
  - zeta records
  - alpha records
outputs:
  - zeta outputs
  - alpha outputs
notes:
  - Keep note order human-owned.
