# Docs Policy

- Keep human-readable governance in `docs/operating_system/`.
- Keep product and platform docs in `docs/` and `docs/features/`.
- Keep generated discovery in `docs/generated/`.
- Generated adapter files should point back to their shared source.
- Public publication must exclude private operating-system and agent-core materials.
