# Collaboration Model

- Use root `AGENTS.md` for repo-wide guidance.
- Use nested `AGENTS.md` only where local behavior truly differs.
- Use skills for reusable workflows, not repo policy.
- Use Codex rules for execution policy, not long-form workflow prose.
- Prefer explicit sync and verification over silent drift.
