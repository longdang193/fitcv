diff --strip-trailing-cr -ruN '--exclude=__pycache__' /mnt/data/fitcv_cp_review/app.py /mnt/data/fitcv_cp_patched/app.py
--- /mnt/data/fitcv_cp_review/app.py	2026-06-25 18:31:44.000000000 +0000
+++ /mnt/data/fitcv_cp_patched/app.py	2026-06-25 23:05:30.349806281 +0000
@@ -36,7 +36,7 @@
 from fastapi import FastAPI, HTTPException, Request, UploadFile, File, Form
 from fastapi.responses import HTMLResponse, RedirectResponse, Response
 from fastapi.templating import Jinja2Templates
-from pydantic import BaseModel, field_validator
+from pydantic import BaseModel, Field, field_validator
 
 from fitcv.config import (
     apply_cv_compatibility_projection,
@@ -76,6 +76,8 @@
     decode_json_object_or_none,
     pretty_json_string,
     pretty_json_string_or_fallback,
+    policy_envelope_signature as _policy_envelope_signature,
+    policy_registry_version as _policy_registry_version_from_config,
     run_mode_label,
 )
 from fitcv_cp.settings_schema import (
@@ -91,6 +93,7 @@
     SETTINGS_SECTIONS,
     ValidationError,
     apply_settings_to_config,
+    canonical_setting_key,
     coerce_value,
     decision_status_sort_key,
     derive_settings_decision_state,
@@ -134,7 +137,7 @@
 from fitcv_cp.data_plane import data_plane_contract_payload
 from fitcv_cp.observability import emit_observability_event
 from fitcv_cp.store import ControlPlaneStore
-from fitcv_cp.review_identity import ensure_review_item_id
+from fitcv_cp.review_identity import ensure_review_item_id, map_review_required_reason_code as _map_review_required_reason_code
 TEMPLATES_DIR = Path(__file__).parent / "templates"
 ORCHESTRATION_ADAPTER = get_orchestration_adapter()
 _RUN_SUBMISSION_CACHE_TTL_SECS = float(os.environ.get("FITCV_CP_SUBMISSION_CACHE_TTL_SECS") or 15 * 60)
@@ -561,6 +564,12 @@
 def get_queue_job_status(queue_job_id: str, redis_url: str = "redis://redis:6379/0") -> str:
     return ORCHESTRATION_ADAPTER.status(queue_job_id=queue_job_id, redis_url=redis_url)
 
+
+def _orchestration_adapter_for_run(run: PipelineRun):
+    backend = str(run.orchestration_backend or "").strip()
+    return get_orchestration_adapter(backend or str(ORCHESTRATION_ADAPTER.name))
+
+
 def continue_run_with_job_id(
     *,
     run_id: str,
@@ -635,7 +644,10 @@
     if backend_run_id and run.status in {RunStatus.QUEUED, RunStatus.RUNNING, RunStatus.CANCELLING}:
         status_checked = True
         try:
-            status = str(orchestration_job_status(backend_run_id, redis_url=redis_url) or "unknown").strip() or "unknown"
+            adapter = _orchestration_adapter_for_run(run)
+            status = str(
+                adapter.status(queue_job_id=backend_run_id, redis_url=redis_url) or "unknown"
+            ).strip() or "unknown"
         except Exception:
             status = "unknown"
     return {
@@ -741,24 +753,7 @@
         "is_archived": bool(run.archived_at),
     }
 
-def _policy_registry_version_from_config(config_payload: dict[str, Any] | None) -> str:
-    cfg = dict(config_payload or {})
-    block = dict(cfg.get("policy_registry") or {})
-    return str(block.get("version") or "policy_registry.v1")
-
-def _policy_envelope_signature(config_payload: dict[str, Any] | None) -> str:
-    cfg = dict(config_payload or {})
-    envelope = {
-        "ranking_weights": dict(cfg.get("ranking_weights") or {}),
-        "preference_fit_weights": dict(cfg.get("preference_fit_weights") or {}),
-        "missing_value_defaults": dict(cfg.get("missing_value_defaults") or {}),
-        "fit_label_thresholds": dict(cfg.get("fit_label_thresholds") or {}),
-        "cv": dict(cfg.get("cv") or {}),
-        "pipeline": dict(cfg.get("pipeline") or {}),
-        "prompts_runtime": dict(cfg.get("prompts_runtime") or {}),
-    }
-    payload = _json.dumps(envelope, sort_keys=True, ensure_ascii=False)
-    return hashlib.sha256(payload.encode("utf-8")).hexdigest()
+
 
 def _checkpoint_replay_context(run: PipelineRun) -> dict[str, Any]:
     payload = _load_json_object(run.checkpoint_payload_json) or {}
@@ -2012,26 +2007,6 @@
     )
 
 
-def _map_review_required_reason_code(record: dict[str, Any]) -> str:
-    explicit_code = str(record.get("review_required_reason_code") or "").strip()
-    from fitcv.pipeline_contracts import ReviewRequiredReasonCode, is_review_required_reason_code
-
-    if is_review_required_reason_code(explicit_code):
-        return explicit_code
-    error = dict(record.get("error") or {})
-    stage = str(error.get("stage") or "").strip().lower()
-    message = str(error.get("message") or record.get("operator_note") or "").strip().lower()
-    if "unsupported requirements require review" in message:
-        return ReviewRequiredReasonCode.UNSUPPORTED_REQUIREMENT_GAP.value
-    if stage == "markdown_quality_review" or "markdown quality" in message:
-        return ReviewRequiredReasonCode.QUALITY_GATE_FAILED.value
-    if stage == "validation" or "validation failed" in message or "guardrail" in message:
-        return ReviewRequiredReasonCode.VALIDATION_GUARDRAIL_FAILED.value
-    if "insufficient evidence" in message or "evidence coverage" in message:
-        return ReviewRequiredReasonCode.EVIDENCE_COVERAGE_INSUFFICIENT.value
-    if stage in {"provider", "llm"} or "provider" in message or "response unusable" in message:
-        return ReviewRequiredReasonCode.PROVIDER_RESPONSE_UNUSABLE.value
-    return ReviewRequiredReasonCode.MANUAL_REVIEW_OTHER.value
 
 def _extract_unsupported_requirements(record: dict[str, Any]) -> list[str]:
     gap_summary = dict(record.get("gap_summary") or {})
@@ -5854,7 +5829,7 @@
     jobs_path: str = "data/sample_jobs.json"
     config_path: str = "config/env.yaml"
     triggered_by: str = "admin"
-    config_overrides: dict[str, Any] = {}
+    config_overrides: dict[str, Any] = Field(default_factory=dict)
     run_mode: str = "run_all"
 
     @field_validator("jobs_path")
@@ -7106,7 +7081,10 @@
 
         target_status, message, error_stage = _timeout_transition_for_run(run, max_runtime_minutes)
         if run.status == RunStatus.QUEUED and run.queue_job_id:
-            cancel_queued_run(run.queue_job_id, redis_url=redis_url)
+            _orchestration_adapter_for_run(run).cancel(
+                queue_job_id=run.queue_job_id,
+                redis_url=redis_url,
+            )
         update_kwargs: dict[str, Any] = {
             "project": project,
             "dataset": dataset,
@@ -7294,14 +7272,13 @@
             completed_stages=[],
         )
         _persist_run_initial(run, bq=bq, project=project, dataset=dataset)
-        _, queue_job_id = enqueue_run_with_job_id(
+        submission = submit_run(
             jobs_path=actual_jobs_path,
             config_path=config_path,
             triggered_by=triggered_by,
             redis_url=redis_url,
             run_id=run_id,
         )
-        submission = _resolve_submission_binding(run_id, queue_job_id)
         _persist_run_orchestration_binding(
             run_id,
             queue_job_id=submission.queue_job_id,
@@ -7484,14 +7461,13 @@
             completed_stages=[],
         )
         _persist_run_initial(run, bq=bq, project=project, dataset=dataset)
-        _, queue_job_id = enqueue_run_with_job_id(
+        submission = submit_run(
             jobs_path=jobs_path,
             config_path=config_path,
             triggered_by=triggered_by,
             redis_url=redis_url,
             run_id=run_id,
         )
-        submission = _resolve_submission_binding(run_id, queue_job_id)
         _persist_run_orchestration_binding(
             run_id,
             queue_job_id=submission.queue_job_id,
@@ -7771,18 +7747,19 @@
     def _coerce_and_validate_single_setting(
         key: str,
         raw_value: Any,
-    ) -> Any:
+    ) -> tuple[str, Any]:
+        canonical_key = canonical_setting_key(key)
         try:
-            coerced = coerce_value(key, raw_value)
+            coerced = coerce_value(canonical_key, raw_value)
         except KeyError:
             raise HTTPException(status_code=422, detail=f"Unknown setting key: {key!r}")
         except (ValueError, TypeError) as exc:
             raise HTTPException(status_code=422, detail=str(exc))
         try:
-            validate_settings({key: coerced})
+            validate_settings({canonical_key: coerced})
         except ValidationError as exc:
             raise HTTPException(status_code=422, detail=str(exc))
-        return coerced
+        return canonical_key, coerced
 
     async def _parse_uploaded_synonym_overlay(
         synonym_overlay_file: UploadFile | None,
@@ -7812,16 +7789,20 @@
 
     @app.post("/settings/{key}", status_code=200)
     def update_setting(key: str, body: SettingUpdate) -> dict:
-        if key in metadata_only_keys:
+        canonical_key = canonical_setting_key(key)
+        if canonical_key in metadata_only_keys:
             raise HTTPException(status_code=422, detail=f"Setting '{key}' is metadata-only and cannot be saved through single-key routes")
-        if key in hidden_deprecated_keys:
+        if canonical_key in hidden_deprecated_keys:
             raise HTTPException(
                 status_code=422,
-                detail=f"Setting '{key}' is hidden_deprecated and cannot be saved through settings routes",
+                detail=f"Setting '{canonical_key}' is hidden_deprecated and cannot be saved through settings routes",
             )
-        coerced = _coerce_and_validate_single_setting(key, body.value)
-        save_setting(key, coerced, updated_by=body.updated_by, bq=bq, project=project, dataset=dataset)
-        return {"key": key, "value": coerced}
+        canonical_key, coerced = _coerce_and_validate_single_setting(canonical_key, body.value)
+        try:
+            save_setting(canonical_key, coerced, updated_by=body.updated_by, bq=bq, project=project, dataset=dataset)
+        except RuntimeError as exc:
+            raise HTTPException(status_code=503, detail=str(exc)) from exc
+        return {"key": canonical_key, "value": coerced}
 
     @app.get("/admin/settings", response_class=HTMLResponse)
     def admin_settings_view(request: Request) -> HTMLResponse:
@@ -7835,32 +7816,33 @@
     @app.post("/admin/settings/{key}", response_class=HTMLResponse)
     async def admin_settings_update_key(request: Request, key: str) -> HTMLResponse:
         from fastapi.responses import RedirectResponse
+        canonical_key = canonical_setting_key(key)
         form = await request.form()
         value = form.get("value", "")
-        if key in metadata_only_keys:
+        if canonical_key in metadata_only_keys:
             active = load_active_settings(bq=bq, project=project, dataset=dataset)
             return templates.TemplateResponse(
                 request=request,
                 name="settings.html",
                 context=_build_settings_context(
                     active,
-                    error=f"Setting '{key}' is metadata-only and cannot be saved through single-key routes",
+                    error=f"Setting '{canonical_key}' is metadata-only and cannot be saved through single-key routes",
                 ),
                 status_code=422,
             )
-        if key in hidden_deprecated_keys:
+        if canonical_key in hidden_deprecated_keys:
             active = load_active_settings(bq=bq, project=project, dataset=dataset)
             return templates.TemplateResponse(
                 request=request,
                 name="settings.html",
                 context=_build_settings_context(
                     active,
-                    error=f"Setting '{key}' is hidden_deprecated and cannot be saved through settings routes",
+                    error=f"Setting '{canonical_key}' is hidden_deprecated and cannot be saved through settings routes",
                 ),
                 status_code=422,
             )
         try:
-            coerced = _coerce_and_validate_single_setting(key, value)
+            canonical_key, coerced = _coerce_and_validate_single_setting(canonical_key, value)
         except HTTPException as exc:
             active = load_active_settings(bq=bq, project=project, dataset=dataset)
             return templates.TemplateResponse(
@@ -7869,7 +7851,16 @@
                 context=_build_settings_context(active, error=str(exc.detail)),
                 status_code=422,
             )
-        save_setting(key, coerced, updated_by="admin", bq=bq, project=project, dataset=dataset)
+        try:
+            save_setting(canonical_key, coerced, updated_by="admin", bq=bq, project=project, dataset=dataset)
+        except RuntimeError as exc:
+            active = load_active_settings(bq=bq, project=project, dataset=dataset)
+            return templates.TemplateResponse(
+                request=request,
+                name="settings.html",
+                context=_build_settings_context(active, error=str(exc)),
+                status_code=503,
+            )
         return RedirectResponse("/admin/settings", status_code=303)
 
     @app.post("/admin/settings/group/{group_name}", response_class=HTMLResponse)
@@ -8238,7 +8229,10 @@
             )
             return {"status": "cancelled", "run_id": run_id}
         if run.status == RunStatus.QUEUED and run.queue_job_id:
-            cancelled_in_queue = cancel_queued_run(run.queue_job_id, redis_url=redis_url)
+            cancelled_in_queue = _orchestration_adapter_for_run(run).cancel(
+                queue_job_id=run.queue_job_id,
+                redis_url=redis_url,
+            )
             if cancelled_in_queue:
                 # Job still in queue — mark directly cancelled
                 request_run_cancel(run_id, "admin", RunStatus.CANCELLED.value, bq, project=project, dataset=dataset)
@@ -8259,26 +8253,11 @@
                     bq, project=project, dataset=dataset,
                 )
                 return {"status": "cancelled", "run_id": run_id}
-        if run.status == RunStatus.QUEUED and run.started_at is None:
-            request_run_cancel(run_id, "admin", RunStatus.CANCELLED.value, bq, project=project, dataset=dataset)
-            append_event(
-                RunEvent(
-                    run_id=run_id, event_id=event_id, stage="cancel_requested",
-                    level="warning", message="Stop requested — cancelled before worker claim",
-                    created_at=now,
-                ),
-                bq, project=project, dataset=dataset,
-            )
-            append_event(
-                RunEvent(
-                    run_id=run_id, event_id=str(uuid.uuid4()), stage="run_cancelled",
-                    level="warning", message="Run cancelled before pipeline execution",
-                    created_at=now,
-                ),
-                bq, project=project, dataset=dataset,
-            )
-            return {"status": "cancelled", "run_id": run_id}
-        # Running (or queued but already claimed) — set cancelling
+        # Queue cancellation can lose a race with worker claim. Do not declare a
+        # terminal cancellation unless the orchestration backend confirms it.
+        # Persist a cancellation request so a claimed worker observes it before
+        # transitioning the run to RUNNING.
+        # Running (or queued but claim status is uncertain) — set cancelling.
         request_run_cancel(run_id, "admin", RunStatus.CANCELLING.value, bq, project=project, dataset=dataset)
         append_event(
             RunEvent(
@@ -8332,7 +8311,10 @@
             if run.status == RunStatus.QUEUED:
                 target_status = RunStatus.CANCELLED.value
                 if run.queue_job_id:
-                    cancelled_in_queue = cancel_queued_run(run.queue_job_id, redis_url=redis_url)
+                    cancelled_in_queue = _orchestration_adapter_for_run(run).cancel(
+                        queue_job_id=run.queue_job_id,
+                        redis_url=redis_url,
+                    )
                     target_status = RunStatus.CANCELLED.value if cancelled_in_queue else RunStatus.CANCELLING.value
                 elif run.started_at is not None:
                     target_status = RunStatus.CANCELLING.value
@@ -8627,7 +8609,7 @@
             checkpoint_payload_json=run.checkpoint_payload_json,
         )
         try:
-            _, queue_job_id = continue_run_with_job_id(
+            submission = continue_run_submission(
                 run_id=run.run_id,
                 jobs_path=run.jobs_path,
                 config_path=run.config_path,
@@ -8673,7 +8655,6 @@
                 status_code=503,
                 detail="Continue enqueue failed; run restored to awaiting_continue. Check queue/redis connectivity and retry.",
             ) from exc
-        submission = _resolve_submission_binding(run.run_id, queue_job_id)
         _persist_run_orchestration_binding(
             run.run_id,
             queue_job_id=submission.queue_job_id,
@@ -8714,12 +8695,13 @@
         run = get_run(run_id, bq, project=project, dataset=dataset)
         if run is None:
             raise HTTPException(status_code=404, detail="Run not found")
-        if run.status == RunStatus.CANCELLED:
-            raise HTTPException(status_code=409, detail="Cancelled runs cannot be retried")
         if getattr(run, "cancel_requested_at", None) is not None:
             raise HTTPException(status_code=409, detail="Cancel requested: run cannot be retried")
-        if run.status not in {RunStatus.FAILED, RunStatus.QUEUED, RunStatus.RUNNING}:
-            raise HTTPException(status_code=409, detail=f"Cannot retry run with status '{run.status.value}'")
+        if run.status != RunStatus.FAILED:
+            raise HTTPException(
+                status_code=409,
+                detail=f"Only failed runs can be retried; current status is '{run.status.value}'",
+            )
 
         store = _resolve_run_store(bq, project=project, dataset=dataset)
         attempt_payloads = store.list_run_attempt_payloads(run_id)
@@ -8737,14 +8719,13 @@
             raise HTTPException(status_code=409, detail="Retry rejected: max_attempts exhausted")
 
         update_run_status(run.run_id, RunStatus.QUEUED, bq, project=project, dataset=dataset)
-        _, queue_job_id = enqueue_run_with_job_id(
+        submission = submit_run(
             jobs_path=run.jobs_path,
             config_path=run.config_path,
             triggered_by="admin_retry",
             redis_url=redis_url,
             run_id=run.run_id,
         )
-        submission = _resolve_submission_binding(run.run_id, queue_job_id)
         _persist_run_orchestration_binding(
             run.run_id,
             queue_job_id=submission.queue_job_id,
diff --strip-trailing-cr -ruN '--exclude=__pycache__' /mnt/data/fitcv_cp_review/bigquery_client.py /mnt/data/fitcv_cp_patched/bigquery_client.py
--- /mnt/data/fitcv_cp_review/bigquery_client.py	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/fitcv_cp_patched/bigquery_client.py	2026-06-25 23:02:18.523132086 +0000
@@ -0,0 +1,49 @@
+"""Canonical BigQuery client construction for all control-plane processes."""
+
+from __future__ import annotations
+
+import logging
+import os
+import warnings
+from pathlib import Path
+from typing import Any
+
+logger = logging.getLogger(__name__)
+
+
+def validate_google_credentials_path() -> None:
+    """Resolve a credentials file when possible; otherwise fall back to ADC."""
+    credentials_path = os.environ.get("GOOGLE_APPLICATION_CREDENTIALS")
+    if not credentials_path:
+        return
+
+    path = Path(credentials_path)
+    if not path.exists():
+        warnings.warn(
+            "GOOGLE_APPLICATION_CREDENTIALS does not exist: "
+            f"{credentials_path}. Falling back to ADC.",
+            RuntimeWarning,
+            stacklevel=2,
+        )
+        os.environ.pop("GOOGLE_APPLICATION_CREDENTIALS", None)
+        return
+    if path.is_dir():
+        candidates = sorted(candidate for candidate in path.glob("*.json") if candidate.is_file())
+        if len(candidates) == 1:
+            os.environ["GOOGLE_APPLICATION_CREDENTIALS"] = str(candidates[0])
+            return
+        warnings.warn(
+            "GOOGLE_APPLICATION_CREDENTIALS points to a directory without a single "
+            f"key JSON file: {credentials_path}. Falling back to ADC.",
+            RuntimeWarning,
+            stacklevel=2,
+        )
+        os.environ.pop("GOOGLE_APPLICATION_CREDENTIALS", None)
+
+
+def build_bigquery_client() -> Any:
+    """Build the canonical BigQuery client used by web, worker, and reconciler."""
+    validate_google_credentials_path()
+    from google.cloud import bigquery
+
+    return bigquery.Client()
diff --strip-trailing-cr -ruN '--exclude=__pycache__' /mnt/data/fitcv_cp_review/bq_store.py /mnt/data/fitcv_cp_patched/bq_store.py
--- /mnt/data/fitcv_cp_review/bq_store.py	2026-06-25 18:31:44.000000000 +0000
+++ /mnt/data/fitcv_cp_patched/bq_store.py	2026-06-25 23:04:18.984584802 +0000
@@ -613,7 +613,7 @@
             jobs_path, config_path, created_at, effective_settings_json,
             run_mode, checkpoint_status, next_stage, last_completed_stage,
             completed_stages_json, checkpoint_payload_json,
-            jobs_input_source, jobs_input_json,
+            jobs_input_source, jobs_input_json, jobs_input_manifest_json,
             candidate_profile_source, candidate_profile_json,
             queue_job_id, orchestration_backend, orchestration_run_id
         )
@@ -622,7 +622,7 @@
             @jobs_path, @config_path, @created_at, @effective_settings_json,
             @run_mode, @checkpoint_status, @next_stage, @last_completed_stage,
             @completed_stages_json, @checkpoint_payload_json,
-            @jobs_input_source, @jobs_input_json,
+            @jobs_input_source, @jobs_input_json, @jobs_input_manifest_json,
             @candidate_profile_source, @candidate_profile_json,
             @queue_job_id, @orchestration_backend, @orchestration_run_id
         )
@@ -653,6 +653,9 @@
             ),
             bq_module.ScalarQueryParameter("jobs_input_source", "STRING", run.jobs_input_source),
             bq_module.ScalarQueryParameter("jobs_input_json", "STRING", run.jobs_input_json),
+            bq_module.ScalarQueryParameter(
+                "jobs_input_manifest_json", "STRING", run.jobs_input_manifest_json
+            ),
             bq_module.ScalarQueryParameter("candidate_profile_source", "STRING", run.candidate_profile_source),
             bq_module.ScalarQueryParameter("candidate_profile_json", "STRING", run.candidate_profile_json),
             bq_module.ScalarQueryParameter("queue_job_id", "STRING", run.queue_job_id),
@@ -666,6 +669,7 @@
         if not (
             _is_unrecognized_column_error(exc, "orchestration_backend")
             or _is_unrecognized_column_error(exc, "orchestration_run_id")
+            or _is_unrecognized_column_error(exc, "jobs_input_manifest_json")
         ):
             raise
         # Backward-compatible insert path before migration lands.
@@ -691,7 +695,11 @@
         """
         legacy_params = [
             param for param in job_config.query_parameters
-            if param.name not in {"orchestration_backend", "orchestration_run_id"}
+            if param.name not in {
+                "orchestration_backend",
+                "orchestration_run_id",
+                "jobs_input_manifest_json",
+            }
         ]
         legacy_job_config = bq_module.QueryJobConfig(query_parameters=legacy_params)
         _execute_query_with_pipeline_runs_retry(bq, legacy_sql, job_config=legacy_job_config)
@@ -1024,14 +1032,18 @@
     project: str,
     dataset: str,
 ) -> dict[str, Any]:
-    """Report whether orchestration-binding columns exist on pipeline_runs."""
+    """Report whether optional pipeline-run contract columns exist."""
     if bq is None:
         return {
             "status": "unknown",
             "missing_columns": [],
             "warning": "sqlite_mode_no_bigquery_schema_check",
         }
-    required_columns = {"orchestration_backend", "orchestration_run_id"}
+    required_columns = {
+        "orchestration_backend",
+        "orchestration_run_id",
+        "jobs_input_manifest_json",
+    }
     sql = (
         f"SELECT column_name FROM `{project}.{dataset}.INFORMATION_SCHEMA.COLUMNS` "
         "WHERE table_name = 'pipeline_runs'"
@@ -1051,7 +1063,7 @@
     return {
         "status": "fallback",
         "missing_columns": missing,
-        "warning": "orchestration_binding_columns_missing",
+        "warning": "pipeline_runs_contract_columns_missing",
     }
 
 
@@ -1866,7 +1878,7 @@
         import json
         import sqlite3
 
-        db_path = str(os.environ.get("FITCV_CP_SQLITE_PATH") or "data/fitcv_cp.sqlite3").strip() or "data/fitcv_cp.sqlite3"
+        db_path = _local_sqlite_path()
         try:
             with sqlite3.connect(db_path) as conn:
                 rows = conn.execute(
diff --strip-trailing-cr -ruN '--exclude=__pycache__' /mnt/data/fitcv_cp_review/main.py /mnt/data/fitcv_cp_patched/main.py
--- /mnt/data/fitcv_cp_review/main.py	2026-05-24 17:44:20.000000000 +0000
+++ /mnt/data/fitcv_cp_patched/main.py	2026-06-25 23:04:27.755599543 +0000
@@ -15,12 +15,11 @@
 
 import logging
 import os
-import warnings
 from pathlib import Path
 from typing import Any
 
-from fitcv_cp.app import create_app
 from fitcv_cp.backend_runtime import resolve_backend_runtime
+from fitcv_cp.bigquery_client import build_bigquery_client
 from fitcv_cp.bq_store import get_pipeline_runs_schema_status
 from fitcv.config import resolve_model_routing_part
 
@@ -45,41 +44,8 @@
         logger.warning("Failed to read .env defaults from %s: %s", dotenv_path, exc)
 
 
-def _validate_google_credentials_path() -> None:
-    """Resolve credentials path when possible; otherwise fall back to ADC."""
-    credentials_path = os.environ.get("GOOGLE_APPLICATION_CREDENTIALS")
-    if not credentials_path:
-        return
-
-    path = Path(credentials_path)
-    if not path.exists():
-        warnings.warn(
-            "GOOGLE_APPLICATION_CREDENTIALS does not exist: "
-            f"{credentials_path}. Falling back to ADC.",
-            RuntimeWarning,
-            stacklevel=2,
-        )
-        os.environ.pop("GOOGLE_APPLICATION_CREDENTIALS", None)
-        return
-    if path.is_dir():
-        candidates = sorted(candidate for candidate in path.glob("*.json") if candidate.is_file())
-        if len(candidates) == 1:
-            os.environ["GOOGLE_APPLICATION_CREDENTIALS"] = str(candidates[0])
-            return
-        warnings.warn(
-            "GOOGLE_APPLICATION_CREDENTIALS points to a directory without a single "
-            f"key JSON file: {credentials_path}. Falling back to ADC.",
-            RuntimeWarning,
-            stacklevel=2,
-        )
-        os.environ.pop("GOOGLE_APPLICATION_CREDENTIALS", None)
-
-
-def _build_bigquery_client() -> Any:
-    _validate_google_credentials_path()
-    from google.cloud import bigquery
-
-    return bigquery.Client()
+# Backward-compatible private alias; implementation is owned by bigquery_client.py.
+_build_bigquery_client = build_bigquery_client
 
 def _warn_or_fail_langgraph_override_drift() -> None:
     """Detect env override drift against control-plane SSOT routing."""
@@ -133,6 +99,8 @@
 def build_app() -> Any:
     _load_dotenv_defaults()
     _ensure_safe_local_execution_mode()
+    # Import after runtime defaults so app module-level resolution sees canonical values.
+    from fitcv_cp.app import create_app
     _warn_or_fail_langgraph_override_drift()
     runtime = resolve_backend_runtime()
     redis_url = os.environ.get("REDIS_URL", "redis://redis:6379/0")
@@ -164,8 +132,8 @@
     elif schema_status.get("status") == "fallback":
         missing = ", ".join(schema_status.get("missing_columns") or [])
         logger.warning(
-            "orchestration schema mode: fallback (missing columns: %s). "
-            "Run migration to add orchestration_backend and orchestration_run_id.",
+            "pipeline_runs schema mode: fallback (missing columns: %s). "
+            "Run the pipeline_runs contract migration before relying on those fields.",
             missing or "unknown",
         )
     else:
diff --strip-trailing-cr -ruN '--exclude=__pycache__' /mnt/data/fitcv_cp_review/orchestrator.py /mnt/data/fitcv_cp_patched/orchestrator.py
--- /mnt/data/fitcv_cp_review/orchestrator.py	2026-05-24 17:44:20.000000000 +0000
+++ /mnt/data/fitcv_cp_patched/orchestrator.py	2026-06-25 23:05:30.342704367 +0000
@@ -314,9 +314,13 @@
             return super().status(queue_job_id=queue_job_id, redis_url=redis_url)
 
 
-def get_orchestration_adapter() -> OrchestrationAdapter:
-    """Resolve orchestration adapter from runtime mode, defaulting to queue."""
-    mode = str(os.environ.get("FITCV_ORCHESTRATION_MODE", "default_queue") or "default_queue").strip().lower()
-    if mode == "prefect":
+def get_orchestration_adapter(mode: str | None = None) -> OrchestrationAdapter:
+    """Resolve an adapter for an explicit persisted backend or current runtime mode."""
+    resolved_mode = str(
+        mode if mode is not None else os.environ.get("FITCV_ORCHESTRATION_MODE", "default_queue")
+    ).strip().lower()
+    if resolved_mode == "prefect":
         return PrefectOrchestrationAdapter(name="prefect")
-    return OrchestrationAdapter(name="default_queue")
+    if resolved_mode in {"", "queue", "default_queue", "rq"}:
+        return OrchestrationAdapter(name="default_queue")
+    raise ValueError(f"Unsupported orchestration backend: {resolved_mode!r}")
diff --strip-trailing-cr -ruN '--exclude=__pycache__' /mnt/data/fitcv_cp_review/queue.py /mnt/data/fitcv_cp_patched/queue.py
--- /mnt/data/fitcv_cp_review/queue.py	2026-05-27 23:33:16.000000000 +0000
+++ /mnt/data/fitcv_cp_patched/queue.py	2026-06-25 23:02:27.296808211 +0000
@@ -66,7 +66,7 @@
 _rq_scheduler_stub.RQScheduler = _real_scheduler.RQScheduler
 from rq.job import Job
 
-_queue: Optional[Queue] = None
+_queues_by_url: dict[str, Queue] = {}
 _INLINE_JOB_STATUS: dict[str, str] = {}
 
 
@@ -208,11 +208,18 @@
 
 
 def get_queue(redis_url: str = "redis://redis:6379/0") -> Queue:
-    global _queue
-    if _queue is None:
-        conn = redis.from_url(redis_url)
-        _queue = Queue("fitcv", connection=conn)
-    return _queue
+    """Return the queue bound to this Redis URL.
+
+    Queue identity must include the connection target; caching a single process-wide
+    instance makes later calls silently use the first URL.
+    """
+    normalized_url = str(redis_url or "redis://redis:6379/0").strip() or "redis://redis:6379/0"
+    queue_instance = _queues_by_url.get(normalized_url)
+    if queue_instance is None:
+        conn = redis.from_url(normalized_url)
+        queue_instance = Queue("fitcv", connection=conn)
+        _queues_by_url[normalized_url] = queue_instance
+    return queue_instance
 
 
 def enqueue_run_with_job_id(
diff --strip-trailing-cr -ruN '--exclude=__pycache__' /mnt/data/fitcv_cp_review/reconciler_service.py /mnt/data/fitcv_cp_patched/reconciler_service.py
--- /mnt/data/fitcv_cp_review/reconciler_service.py	2026-05-27 23:33:16.000000000 +0000
+++ /mnt/data/fitcv_cp_patched/reconciler_service.py	2026-06-25 23:02:19.569042759 +0000
@@ -22,6 +22,7 @@
 import time
 
 from fitcv_cp.backend_runtime import resolve_backend_runtime
+from fitcv_cp.bigquery_client import build_bigquery_client
 from fitcv_cp.reconciler import reconcile_abandoned_attempts
 from fitcv_cp.retry_settings import load_retry_settings
 from fitcv_cp.store import ControlPlaneStore
@@ -35,9 +36,7 @@
         os.environ.setdefault("FITCV_CP_SQLITE_PATH", runtime.sqlite_path)
         return ControlPlaneStore(bq=None, project=str(runtime.project or "local"), dataset=str(runtime.dataset or "local"))
 
-    from fitcv_cp.main import _build_bigquery_client
-
-    bq = _build_bigquery_client()
+    bq = build_bigquery_client()
     return ControlPlaneStore(bq=bq, project=str(runtime.project), dataset=str(runtime.dataset))
 
 
diff --strip-trailing-cr -ruN '--exclude=__pycache__' /mnt/data/fitcv_cp_review/review_identity.py /mnt/data/fitcv_cp_patched/review_identity.py
--- /mnt/data/fitcv_cp_review/review_identity.py	2026-05-20 12:37:48.000000000 +0000
+++ /mnt/data/fitcv_cp_patched/review_identity.py	2026-06-25 23:05:05.065166313 +0000
@@ -75,3 +75,26 @@
 def is_review_resolution_pending(resolution_status: Any) -> bool:
     normalized = str(resolution_status or "").strip().lower() or "pending"
     return normalized not in TERMINAL_REVIEW_RESOLUTION_STATUSES
+
+
+def map_review_required_reason_code(record: dict[str, Any]) -> str:
+    """Map legacy review records to the canonical pipeline reason code."""
+    explicit_code = str(record.get("review_required_reason_code") or "").strip()
+    from fitcv.pipeline_contracts import ReviewRequiredReasonCode, is_review_required_reason_code
+
+    if is_review_required_reason_code(explicit_code):
+        return explicit_code
+    error = dict(record.get("error") or {})
+    stage = str(error.get("stage") or "").strip().lower()
+    message = str(error.get("message") or record.get("operator_note") or "").strip().lower()
+    if "unsupported requirements require review" in message:
+        return ReviewRequiredReasonCode.UNSUPPORTED_REQUIREMENT_GAP.value
+    if stage == "markdown_quality_review" or "markdown quality" in message:
+        return ReviewRequiredReasonCode.QUALITY_GATE_FAILED.value
+    if stage == "validation" or "validation failed" in message or "guardrail" in message:
+        return ReviewRequiredReasonCode.VALIDATION_GUARDRAIL_FAILED.value
+    if "insufficient evidence" in message or "evidence coverage" in message:
+        return ReviewRequiredReasonCode.EVIDENCE_COVERAGE_INSUFFICIENT.value
+    if stage in {"provider", "llm"} or "provider" in message or "response unusable" in message:
+        return ReviewRequiredReasonCode.PROVIDER_RESPONSE_UNUSABLE.value
+    return ReviewRequiredReasonCode.MANUAL_REVIEW_OTHER.value
diff --strip-trailing-cr -ruN '--exclude=__pycache__' /mnt/data/fitcv_cp_review/run_artifact_contracts.py /mnt/data/fitcv_cp_patched/run_artifact_contracts.py
--- /mnt/data/fitcv_cp_review/run_artifact_contracts.py	2026-05-27 23:33:16.000000000 +0000
+++ /mnt/data/fitcv_cp_patched/run_artifact_contracts.py	2026-06-25 23:05:13.767919366 +0000
@@ -16,6 +16,7 @@
 
 from __future__ import annotations
 
+import dataclasses
 import datetime
 import hashlib
 import json as _json
@@ -50,6 +51,8 @@
 
 
 def json_safe(value: Any) -> Any:
+    if dataclasses.is_dataclass(value):
+        return {str(k): json_safe(v) for k, v in dataclasses.asdict(value).items()}
     if isinstance(value, datetime.datetime):
         return value.isoformat()
     if isinstance(value, datetime.date):
@@ -95,6 +98,30 @@
     return hashlib.sha256(raw.encode("utf-8")).hexdigest()
 
 
+_POLICY_ENVELOPE_KEYS = (
+    "ranking_weights",
+    "preference_fit_weights",
+    "missing_value_defaults",
+    "fit_label_thresholds",
+    "cv",
+    "pipeline",
+    "prompts_runtime",
+)
+
+
+def policy_registry_version(config_payload: dict[str, Any] | None) -> str:
+    cfg = dict(config_payload or {})
+    block = dict(cfg.get("policy_registry") or {})
+    return str(block.get("version") or "policy_registry.v1")
+
+
+def policy_envelope_signature(config_payload: dict[str, Any] | None) -> str:
+    """Fingerprint the policy fields that define replay semantics."""
+    cfg = dict(config_payload or {})
+    envelope = {key: dict(cfg.get(key) or {}) for key in _POLICY_ENVELOPE_KEYS}
+    return stable_sha256_fingerprint(envelope)
+
+
 def require_payload_keys(
     payload: dict[str, Any],
     *,
diff --strip-trailing-cr -ruN '--exclude=__pycache__' /mnt/data/fitcv_cp_review/run_artifact_mirror.py /mnt/data/fitcv_cp_patched/run_artifact_mirror.py
--- /mnt/data/fitcv_cp_review/run_artifact_mirror.py	2026-05-17 19:00:04.000000000 +0000
+++ /mnt/data/fitcv_cp_patched/run_artifact_mirror.py	2026-06-25 23:05:05.256320637 +0000
@@ -15,8 +15,6 @@
 
 from __future__ import annotations
 
-import dataclasses
-import datetime
 import json
 import logging
 from pathlib import Path
@@ -24,26 +22,11 @@
 
 from fitcv_cp.bq_store import get_events, get_run
 from fitcv_cp.models import RunEvent, RunStatus
+from fitcv_cp.run_artifact_contracts import json_safe
 
 logger = logging.getLogger(__name__)
 
 
-def _json_safe(value: Any) -> Any:
-    if isinstance(value, datetime.datetime):
-        return value.isoformat()
-    if isinstance(value, datetime.date):
-        return value.isoformat()
-    if dataclasses.is_dataclass(value):
-        return {str(k): _json_safe(v) for k, v in dataclasses.asdict(value).items()}
-    if isinstance(value, dict):
-        return {str(k): _json_safe(v) for k, v in value.items()}
-    if isinstance(value, list):
-        return [_json_safe(item) for item in value]
-    if isinstance(value, tuple):
-        return [_json_safe(item) for item in value]
-    if isinstance(value, set):
-        return [_json_safe(item) for item in sorted(value)]
-    return value
 
 
 def _write_json_atomic(path: Path, payload: Any) -> None:
@@ -59,8 +42,8 @@
     events: list[RunEvent],
 ) -> dict[str, Any]:
     payloads: dict[str, Any] = {
-        "run.json": _json_safe(run_record),
-        "events.json": [_json_safe(event) for event in events],
+        "run.json": json_safe(run_record),
+        "events.json": [json_safe(event) for event in events],
     }
     results_export_raw = getattr(run_record, "results_export_json", None)
     if isinstance(results_export_raw, str) and results_export_raw.strip():
diff --strip-trailing-cr -ruN '--exclude=__pycache__' /mnt/data/fitcv_cp_review/settings_schema.py /mnt/data/fitcv_cp_patched/settings_schema.py
--- /mnt/data/fitcv_cp_review/settings_schema.py	2026-06-16 23:18:16.000000000 +0000
+++ /mnt/data/fitcv_cp_patched/settings_schema.py	2026-06-25 23:03:49.906853219 +0000
@@ -1672,14 +1672,28 @@
     "enrichment_concurrency": "stage_runtime.enrich.concurrency",
 }
 
+
+def canonical_setting_key(key: str) -> str:
+    """Return the one persisted/runtime key for a compatibility alias."""
+    normalized = str(key or "").strip()
+    return _LEGACY_THROUGHPUT_ALIAS_TO_CANONICAL.get(normalized, normalized)
+
+
 def _normalize_settings_aliases(settings: dict[str, Any]) -> dict[str, Any]:
-    """Apply canonical-over-legacy precedence for throughput compatibility aliases."""
-    normalized = dict(settings)
+    """Collapse compatibility aliases into canonical keys.
+
+    Canonical values win when both spellings are present, independent of input
+    order. Legacy spellings are never returned, so downstream config application
+    and persistence have a single source of truth.
+    """
+    normalized = {
+        str(key): value
+        for key, value in settings.items()
+        if str(key) not in _LEGACY_THROUGHPUT_ALIAS_TO_CANONICAL
+    }
     for legacy_key, canonical_key in _LEGACY_THROUGHPUT_ALIAS_TO_CANONICAL.items():
-        if canonical_key in normalized:
-            continue
-        if legacy_key in normalized:
-            normalized[canonical_key] = normalized[legacy_key]
+        if canonical_key not in normalized and legacy_key in settings:
+            normalized[canonical_key] = settings[legacy_key]
     return normalized
 
 
diff --strip-trailing-cr -ruN '--exclude=__pycache__' /mnt/data/fitcv_cp_review/settings_store.py /mnt/data/fitcv_cp_patched/settings_store.py
--- /mnt/data/fitcv_cp_review/settings_store.py	2026-05-24 20:05:32.000000000 +0000
+++ /mnt/data/fitcv_cp_patched/settings_store.py	2026-06-25 23:03:49.907499536 +0000
@@ -22,7 +22,7 @@
 from pathlib import Path
 from typing import Any
 
-from fitcv_cp.settings_schema import coerce_value, editable_settings_keys
+from fitcv_cp.settings_schema import canonical_setting_key, coerce_value, editable_settings_keys
 
 logger = logging.getLogger(__name__)
 
@@ -468,8 +468,9 @@
     dataset: str,
 ) -> None:
     """Append a new row for this key. Current value = latest row per key."""
+    canonical_key = canonical_setting_key(key)
     row = {
-        "setting_key": key,
+        "setting_key": canonical_key,
         "setting_value_json": json.dumps(value),
         "updated_by": updated_by,
         "updated_at": datetime.datetime.now(datetime.timezone.utc).isoformat(),
@@ -481,6 +482,7 @@
     errors = bq.insert_rows_json(table, [row])
     if errors:
         logger.error("BQ save_setting errors: %s", errors)
+        raise RuntimeError(f"Failed to save setting {canonical_key!r}: {errors}")
 
 
 def save_settings_group(
@@ -502,6 +504,9 @@
     partial failures are possible but accepted for this admin tool.
     """
     now = datetime.datetime.now(datetime.timezone.utc).isoformat()
+    canonical_values: dict[str, Any] = {}
+    for key, value in keys_values.items():
+        canonical_values[canonical_setting_key(key)] = value
     rows = [
         {
             "setting_key": key,
@@ -509,7 +514,7 @@
             "updated_by": updated_by,
             "updated_at": now,
         }
-        for key, value in keys_values.items()
+        for key, value in canonical_values.items()
     ]
     if bq is None:
         _save_local_settings_rows(rows)
@@ -539,15 +544,21 @@
     seen_valid: set[str] = set()
     result: dict[str, Any] = {}
     for row in rows:
-        key = str(row["setting_key"])
+        raw_key = str(row["setting_key"])
+        key = canonical_setting_key(raw_key)
         if key in seen_valid:
-            continue  # older value for same key — skip
+            continue  # older canonical value or alias-family value — skip
         raw = json.loads(str(row["setting_value_json"]))
         try:
             result[key] = coerce_value(key, raw)
             seen_valid.add(key)
         except (KeyError, ValueError) as exc:
-            logger.warning("Skipping unknown/invalid setting key=%s: %s", key, exc)
+            logger.warning(
+                "Skipping unknown/invalid setting key=%s (canonical=%s): %s",
+                raw_key,
+                key,
+                exc,
+            )
 
     return result
 
diff --strip-trailing-cr -ruN '--exclude=__pycache__' /mnt/data/fitcv_cp_review/templates/base.html /mnt/data/fitcv_cp_patched/templates/base.html
--- /mnt/data/fitcv_cp_review/templates/base.html	2026-06-25 18:01:32.000000000 +0000
+++ /mnt/data/fitcv_cp_patched/templates/base.html	2026-06-25 23:06:35.018520891 +0000
@@ -27,6 +27,10 @@
   <title>{% block title %}FitCV Admin{% endblock %}</title>
   <style>
     /* ── CSS Custom Properties (tokens) ──────────────────────────────────── */
+    :root {
+      --font-sans: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
+      --font-mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
+    }
     :root[data-theme="dark"] {
       --bg:          #0f1117;
       --surface-1:   #1a1d2e;
@@ -91,7 +95,7 @@
     /* ── Reset & base ─────────────────────────────────────────────────────── */
     *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
     body {
-      font-family: 'Inter', system-ui, sans-serif;
+      font-family: var(--font-sans);
       background: var(--bg);
       color: var(--text-primary);
       min-height: 100vh;
@@ -376,7 +380,9 @@
 
     /* Legacy fallback — default button style (same as primary) */
     button { background: var(--accent); color: #fff; border: none; padding: 0.5rem 1.1rem; border-radius: 6px; font-size: 0.875rem; font-weight: 600; font-family: inherit; cursor: pointer; transition: background 0.15s; }
-    button:not(.btn-secondary):hover { background: var(--accent-hover); }
+    button:not(.btn-secondary):not(.btn-section):not(.tab-btn):not(.axis-chip):not(.decision-toggle):hover {
+      background: var(--accent-hover);
+    }
 
     /* ── Badges ───────────────────────────────────────────────────────────── */
     .badge {
@@ -439,7 +445,7 @@
       border: 1px solid var(--code-border);
       border-radius: 6px;
       font-size: 0.8rem;
-      font-family: monospace;
+      font-family: var(--font-mono);
     }
     code { padding: 0.1rem 0.35rem; }
     .code-area { padding: 0.5rem; }
diff --strip-trailing-cr -ruN '--exclude=__pycache__' /mnt/data/fitcv_cp_review/worker_job.py /mnt/data/fitcv_cp_patched/worker_job.py
--- /mnt/data/fitcv_cp_review/worker_job.py	2026-05-27 23:33:16.000000000 +0000
+++ /mnt/data/fitcv_cp_patched/worker_job.py	2026-06-25 23:05:05.254821251 +0000
@@ -72,7 +72,7 @@
     evaluate_synonym_triage_reuse,
     transition_synonym_proposal_status,
 )
-from fitcv_cp.review_identity import ensure_review_item_id, is_review_resolution_pending
+from fitcv_cp.review_identity import ensure_review_item_id, map_review_required_reason_code as _map_review_required_reason_code, is_review_resolution_pending
 from fitcv_cp.retry_policy import classify_exception_for_retry
 from fitcv_cp.run_artifact_contracts import (
     encode_json_object,
@@ -88,6 +88,8 @@
     stable_json_dumps,
     stable_sha256_fingerprint,
     string_or_none,
+    policy_envelope_signature as _policy_envelope_signature,
+    policy_registry_version as _policy_registry_version,
 )
 
 logger = logging.getLogger(__name__)
@@ -158,24 +160,7 @@
         "outcome_counts": {},
     }
 
-def _policy_registry_version(config_payload: dict[str, Any] | None) -> str:
-    cfg = dict(config_payload or {})
-    block = dict(cfg.get("policy_registry") or {})
-    return str(block.get("version") or "policy_registry.v1")
-
-def _policy_envelope_signature(config_payload: dict[str, Any] | None) -> str:
-    cfg = dict(config_payload or {})
-    envelope = {
-        "ranking_weights": dict(cfg.get("ranking_weights") or {}),
-        "preference_fit_weights": dict(cfg.get("preference_fit_weights") or {}),
-        "missing_value_defaults": dict(cfg.get("missing_value_defaults") or {}),
-        "fit_label_thresholds": dict(cfg.get("fit_label_thresholds") or {}),
-        "cv": dict(cfg.get("cv") or {}),
-        "pipeline": dict(cfg.get("pipeline") or {}),
-        "prompts_runtime": dict(cfg.get("prompts_runtime") or {}),
-    }
-    raw = json.dumps(envelope, sort_keys=True, ensure_ascii=False)
-    return hashlib.sha256(raw.encode("utf-8")).hexdigest()
+
 
 def _resolve_run_replay_context(
     *,
@@ -1207,26 +1192,6 @@
     path.write_text(updated, encoding="utf-8")
 
 
-def _map_review_required_reason_code(record: dict[str, Any]) -> str:
-    explicit_code = str(record.get("review_required_reason_code") or "").strip()
-    from fitcv.pipeline_contracts import ReviewRequiredReasonCode, is_review_required_reason_code
-
-    if is_review_required_reason_code(explicit_code):
-        return explicit_code
-    error = dict(record.get("error") or {})
-    stage = str(error.get("stage") or "").strip().lower()
-    message = str(error.get("message") or record.get("operator_note") or "").strip().lower()
-    if "unsupported requirements require review" in message:
-        return ReviewRequiredReasonCode.UNSUPPORTED_REQUIREMENT_GAP.value
-    if stage == "markdown_quality_review" or "markdown quality" in message:
-        return ReviewRequiredReasonCode.QUALITY_GATE_FAILED.value
-    if stage == "validation" or "validation failed" in message or "guardrail" in message:
-        return ReviewRequiredReasonCode.VALIDATION_GUARDRAIL_FAILED.value
-    if "insufficient evidence" in message or "evidence coverage" in message:
-        return ReviewRequiredReasonCode.EVIDENCE_COVERAGE_INSUFFICIENT.value
-    if stage in {"provider", "llm"} or "provider" in message or "response unusable" in message:
-        return ReviewRequiredReasonCode.PROVIDER_RESPONSE_UNUSABLE.value
-    return ReviewRequiredReasonCode.MANUAL_REVIEW_OTHER.value
 
 
 def _summary_has_reached_stage(summary: dict[str, Any], stage_id: str) -> bool:
@@ -1978,6 +1943,34 @@
     ):
         try:
             current_run_record = get_run(run_id, bq, project=project, dataset=dataset)
+            if current_run_record is not None and (
+                current_run_record.status in {RunStatus.CANCELLED, RunStatus.CANCELLING}
+                or getattr(current_run_record, "cancel_requested_at", None) is not None
+            ):
+                cancelled_at = datetime.datetime.now(datetime.timezone.utc)
+                if current_run_record.status != RunStatus.CANCELLED:
+                    update_run_status(
+                        run_id,
+                        RunStatus.CANCELLED,
+                        bq,
+                        project=project,
+                        dataset=dataset,
+                        finished_at=cancelled_at,
+                    )
+                append_event(
+                    RunEvent(
+                        run_id=run_id,
+                        event_id=str(uuid.uuid4()),
+                        stage="run_cancelled",
+                        level="warning",
+                        message="Run cancelled before worker execution began",
+                        created_at=cancelled_at,
+                    ),
+                    bq,
+                    project=project,
+                    dataset=dataset,
+                )
+                return
             # ── Step 1: Mark running ──────────────────────────────────────────────
             update_run_status(
                 run_id, RunStatus.RUNNING, bq, project=project, dataset=dataset,
