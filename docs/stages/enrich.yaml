enrich:
  stage_id: enrich
  name: Enrich
  summary: Enrich normalized jobs into the stable downstream job contract and pair that job shape with the candidate profile/runtime context used by later stages.
  boundaries:
    - normalized jobs enter and enriched job rows leave
    - canonical downstream fields such as required skills, job family, and years fields become usable here
    - downstream scoring and CV work should rely on the enriched contract rather than raw input shape
  inputs:
    - normalized job rows
    - candidate profile or runtime profile snapshot
  outputs:
    - enriched job rows
    - canonical downstream job fields
    - runtime candidate profile context for later pipeline stages
    - bounded `stage_transition_artifacts.enrich` runtime block with input, output, and changed-state samples
  depends_on:
    - normalize
  primary_features:
    - cv_system
  related_features:
    - trigger_run_management
    - inspection_debugging
  refs:
    docs: []
    spec:
      - docs/superpowers/specs/2026-03-31-stage-contract-docs-adoption-design.md
      - docs/superpowers/specs/2026-03-31-stage-transition-artifacts-design.md
      - docs/superpowers/specs/2026-04-01-richer-stage-artifacts-design.md
    plan:
      - docs/superpowers/plans/2026-03-31-stage-transition-artifacts-implementation.md
      - docs/superpowers/plans/2026-04-01-richer-stage-artifacts-implementation.md
    history: []
  keywords:
    - enrich
    - candidate profile
    - required skills
    - years experience
