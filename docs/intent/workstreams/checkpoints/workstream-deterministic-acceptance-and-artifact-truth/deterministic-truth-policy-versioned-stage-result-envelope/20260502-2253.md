# Checkpoint Result Pack

- Workstream ID: `workstream-deterministic-acceptance-and-artifact-truth`
- Thread Slug: `deterministic-truth-policy-versioned-stage-result-envelope`
- Checkpoint ID: `workstream-deterministic-acceptance-and-artifact-truth.deterministic-truth-policy-versioned-stage-result-envelope.20260502-2253`
- Execution pass timestamp (UTC): `2026-05-02T22:53:00Z`

## Intent

Add a machine-readable outbox replay trend export so operators and automation can consume cross-run dead-letter/replay health without parsing HTML.

## Actions

- Added new endpoint in `src/fitcv_cp/app.py`:
  - `GET /admin/outbox-replay-health.json`
  - supports `view=active|all|archived`
  - returns `run_count`, `generated_at`, and `outbox_replay_health` payload
- Reused existing aggregate dead-letter/replay health computation to keep UI and API consistent.
- Added JSON endpoint test in `tests/test_fitcv_cp/test_app.py` for degraded aggregate scenario.

## Visible Output

- `src/fitcv_cp/app.py`
- `tests/test_fitcv_cp/test_app.py`
- `docs/intent/workstreams/checkpoints/workstream-deterministic-acceptance-and-artifact-truth/deterministic-truth-policy-versioned-stage-result-envelope/20260502-2253.md`

## Verification Evidence

- `python -m pytest tests/test_fitcv_cp/test_app.py -k "outbox_replay_health_json or degraded_outbox_replay_health"` → `2 passed`
- `python scripts/validate_checkpoint_packs.py` → pass
- `python scripts/validate_repo_contracts.py --fast` → pass

## Status

`pass`

## Next Decision

Proceed to add a small UI/API linkage affordance (download link and contract note) so this export is discoverable from the runs list.
