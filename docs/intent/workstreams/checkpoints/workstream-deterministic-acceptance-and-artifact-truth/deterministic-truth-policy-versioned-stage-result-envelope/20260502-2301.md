# Checkpoint Result Pack

- Workstream ID: `workstream-deterministic-acceptance-and-artifact-truth`
- Thread Slug: `deterministic-truth-policy-versioned-stage-result-envelope`
- Checkpoint ID: `workstream-deterministic-acceptance-and-artifact-truth.deterministic-truth-policy-versioned-stage-result-envelope.20260502-2301`
- Execution pass timestamp (UTC): `2026-05-02T23:01:00Z`

## Intent

Turn outbox replay observability into active control by adding threshold-based health checks with auditable control-plane alert events.

## Actions

- Added control-plane alert-check endpoint in `src/fitcv_cp/app.py`:
  - `POST /admin/outbox-replay-health/check`
  - evaluates `status=degraded` OR `replay_success_ratio < min_replay_success_ratio`
  - returns `decision: ok|alert`, `reason_code`, and aggregate health payload
  - emits `outbox_replay_health_alert` event via `append_event` (warning/info)
- Added scheduler-friendly checker script:
  - `scripts/check_outbox_replay_health.py`
  - calls endpoint, prints JSON, exits `2` on alert, `0` on ok, `3` on request failure
- Added tests for endpoint behavior and event emission levels in `tests/test_fitcv_cp/test_app.py`.
- Ran `scripts/sync_architecture_docs.py` because repo-contract validation flagged stale generated docs.

## Visible Output

- `src/fitcv_cp/app.py`
- `scripts/check_outbox_replay_health.py`
- `tests/test_fitcv_cp/test_app.py`
- `docs/intent/workstreams/checkpoints/workstream-deterministic-acceptance-and-artifact-truth/deterministic-truth-policy-versioned-stage-result-envelope/20260502-2301.md`

## Verification Evidence

- `python -m pytest tests/test_fitcv_cp/test_app.py -k "outbox_replay_health_check or outbox_replay_health_json"` → `3 passed`
- `python scripts/validate_checkpoint_packs.py` → pass
- `python scripts/validate_repo_contracts.py --fast` → pass

## Status

`pass`

## Next Decision

Proceed to wire this checker into a recurring scheduler/automation and add operator documentation for exit-code handling.
