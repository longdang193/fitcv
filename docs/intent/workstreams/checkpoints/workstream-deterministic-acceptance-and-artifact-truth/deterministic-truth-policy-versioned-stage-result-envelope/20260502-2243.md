# Checkpoint Result Pack

- Workstream ID: `workstream-deterministic-acceptance-and-artifact-truth`
- Thread Slug: `deterministic-truth-policy-versioned-stage-result-envelope`
- Checkpoint ID: `workstream-deterministic-acceptance-and-artifact-truth.deterministic-truth-policy-versioned-stage-result-envelope.20260502-2243`
- Execution pass timestamp (UTC): `2026-05-02T22:43:00Z`

## Intent

Execute the next reliability slice after Plan F pass 3 by adding cross-run outbox/dead-letter trend visibility on the runs list for faster operator triage.

## Actions

- Added aggregate helper in `src/fitcv_cp/app.py` to compute visible-run replay health from:
  - dead-letter records (`FITCV_EVENT_DEAD_LETTER_PATH`)
  - latest per-run `event_dead_letter_replay` event payloads
- Extended `/admin/runs` context with `dead_letter_replay_health`.
- Added a new runs-list card: **Outbox Replay Health (Visible Runs)** with status, dead-letter totals, impacted runs, replay ratio, and failure counts.
- Extended tests in `tests/test_fitcv_cp/test_app.py`:
  - baseline nav test now verifies the new card is rendered
  - new degraded aggregate scenario test across multiple runs

## Visible Output

- `src/fitcv_cp/app.py`
- `src/fitcv_cp/templates/runs_list.html`
- `tests/test_fitcv_cp/test_app.py`
- `docs/intent/workstreams/checkpoints/workstream-deterministic-acceptance-and-artifact-truth/deterministic-truth-policy-versioned-stage-result-envelope/20260502-2243.md`

## Verification Evidence

- `python -m pytest tests/test_fitcv_cp/test_app.py -k "admin_runs_rendered_nav or degraded_outbox_replay_health"` → `2 passed`
- `python scripts/validate_checkpoint_packs.py` → pass
- `python scripts/validate_repo_contracts.py --fast` → pass

## Status

`pass`

## Next Decision

Proceed to the next pass: add export/API surface for outbox replay trend snapshots so this aggregate can be consumed outside UI.
