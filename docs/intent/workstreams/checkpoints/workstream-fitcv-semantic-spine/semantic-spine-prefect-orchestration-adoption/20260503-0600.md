# Checkpoint Result Pack

- Workstream ID: `workstream-fitcv-semantic-spine`
- Thread Slug: `semantic-spine-prefect-orchestration-adoption`
- Checkpoint ID: `workstream-fitcv-semantic-spine.semantic-spine-prefect-orchestration-adoption.20260503-0600`
- Execution pass timestamp (UTC): `2026-05-03T06:00:00Z`

## Intent

Execute the next bounded pass after Plan I pass 10 by running the operator verification workflow against a live branch-built control-plane instance and capturing queue/prefect diagnostics evidence.

## Actions

- Detected existing local servers on `8000` and `8010`, then confirmed both were stale for this scope because `/admin/diagnostics/orchestration-schema` was missing from their OpenAPI paths.
- Started branch-isolated control-plane server on `http://localhost:8030` from this worktree using explicit credentials and environment bindings.
- Ran `scripts/verify_fitcv_orchestration_modes.ps1` on `8030`.
- Fixed script evidence-surface mismatch:
  - removed incorrect reliance on `/runs` orchestration fields (not exposed by current API model)
  - backend presence checks now use `/admin/runs` orchestration UI cells
  - kept schema diagnostics endpoint + run-detail orchestration labels as required assertions
- Re-ran baseline and strict mixed-backend verification modes.

## Visible Output

- `scripts/verify_fitcv_orchestration_modes.ps1`
- `docs/intent/workstreams/checkpoints/workstream-fitcv-semantic-spine/semantic-spine-prefect-orchestration-adoption/20260503-0600.md`

## Verification Evidence

- `powershell -ExecutionPolicy Bypass -File .\scripts\verify_fitcv_orchestration_modes.ps1 -BaseUrl "http://localhost:8030"`:
  - `schema-required-columns` pass
  - `schema-status` pass (`fallback`)
  - `runs-available` pass
  - run-detail orchestration labels pass
  - overall pass
- `powershell -ExecutionPolicy Bypass -File .\scripts\verify_fitcv_orchestration_modes.ps1 -BaseUrl "http://localhost:8030" -RequireQueue -RequirePrefect`:
  - `queue` present
  - `prefect` missing (`backends=default_queue`)
  - overall fail as expected until a Prefect-backed run exists

## Status

`partial`

## Next Decision

Create at least one Prefect-backed run in this environment (with valid Prefect configuration), then rerun strict mixed-backend verification and publish a pass checkpoint proving `default_queue` + `prefect` coexistence diagnostics.
