Audit trigger assessment:
- Trigger met: #5 contract/invariant drift where failure boundary is unclear.
- Reason: overlapping runtime keys exist across `config/env.private.yaml` and `config/runtime/pipeline.yaml`, while `config/runtime/control_plane.yaml` separately defines provider/model routing and backend settings.

Observed overlaps (examples):
- `gemini_model` in env.private + pipeline
- `embedding_model` in env.private + pipeline
- `enrichment_sleep_secs` in env.private + pipeline
- `vector_top_n` in env.private + pipeline
- `retrieval_strategy` in env.private + pipeline
- `pipeline.vector_search_top_n|ai_score_top_n|final_top_n|evidence_top_k` in env.private + pipeline

Risk:
- effective runtime source-of-truth unclear when both files define same key and load order is implicit
- can cause silent config drift between pipeline behavior and operator expectations

Boundary hypothesis:
- Contract boundary between global/env config layer and runtime pipeline config layer.
- Components: config loaders + callers in `src/fitcv/*` and control-plane runtime wiring.
