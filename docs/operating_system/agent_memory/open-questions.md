# Open Questions

- Should the repo eventually add a CI or lint-style freshness check for the memory layer itself, or keep it intentionally manual for now?
- Should recurring agent-memory entries eventually feed dedicated rules, skills, or structural checks automatically, or stay human-curated until the pattern volume grows?
