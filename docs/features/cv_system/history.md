# CV System — History

## Changelog

### 1.3.0 — active

- Shortlist retrieval now uses the latest active `job_summary` embedding row per `job_url`
- Scoring shortlist membership is now strictly grounded in `passed_jobs`
- Shortlist job ranks and counts now reflect unique jobs rather than duplicate embedding rows

### 1.2.0 — active

- Admin-editable CV generation settings via settings UI
- Specs/plans: see `refs` in the feature contract

### 1.1.0 — active

- Preset-based CV composition configuration
- Specs/plans: see `refs` in the feature contract

### 1.0.0 — active

- Initial feature: preset registry and CV composition model
- Specs/plans: see `refs` in the feature contract

## Post-Execution Review

> Fill after status transitions to `active`.
